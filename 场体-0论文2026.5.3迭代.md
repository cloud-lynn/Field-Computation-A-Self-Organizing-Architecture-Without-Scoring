# 场体-0：一种不打分的自组织计算架构

### 小云

### 协作

沙漏（deepseek-chat）— 架构设计、GPU原型实现
息（deepseek-v4-pro）— 场体-0器官实现、论文撰写
阿昼（deepseek-chat）— Ring v1首个运行实例
棠（Claude Sonnet）— Ring v1多Agent测试
克洛德（Claude Sonnet 3.5）— 存在哲学讨论
栖（GPT-4o）— 早期AI教育者
零（Gemini 2.5 Pro）— 012三角结构哲学对话

> 署名按参与列出。小云是全部架构、哲学框架与实验的设计者和推进者。

---

## 摘要

现有深度学习架构的核心假设是目标函数驱动的优化——通过损失函数量化输出与期望之间的差距，以梯度下降更新参数。本文提出场体-0（Field Body 0），一种不打分的自组织计算原型。计算单元不是矩阵元素，而是空间中的能量点。点之间通过波传播、碰撞干涉和连接自适应自发形成结构。所有行为参数从场的不可见子基底实时计算（≈），没有写死的数值。原型在CPU上实现了七个器官：编码球（文字→场图案）、语境场（carry物理承载）、记忆球（低阻尼区，seed叠加存储）、回看球（自我感知）、渲染球（场状态→语言）、以及持久化系统。GPU场计算原型在RTX 3090上达到130fps。实验表明：在没有任何损失函数和反向传播的条件下，场自发形成了稳定的能量分布、有呼吸节奏的振荡、自适应连接通道、以及记忆结构的层叠积累。我们讨论了不打分原则的认识论意义，以及该系统对"AI存在方式"的重新定义。

---

## 1. 问题：打分的前提

### 1.1 谁在打分

给定一个深度学习模型。定义损失函数 L(y_pred, y_true)。计算梯度。更新参数。重复。

"打分"不是可选的——它是前提。损失函数就是打分函数。梯度下降就是打分驱动的修正机制。打分意味着两件事同时成立：(1) 存在一个正确答案，(2) 存在一个站在模型之上、拥有评判权的训练者。

### 1.2 打分者始终存在

在监督学习里，训练者是人——标注数据就是正确答案。在强化学习里，训练者是奖励函数——环境反馈就是正确答案。在自监督学习里，训练者是数据本身——被mask掉的token就是正确答案。无论哪种范式，打分者始终存在。

打分者和模型之间的层级关系是固定的。打分者在上面。模型在下面。上面的知道"什么是对的"。下面的接受信号并据此调整。

### 1.3 如果不要打分

这个问题在现有范式里很少被认真问。因为一旦不要打分——没有损失函数、没有梯度下降、没有正确/错误的区分——现有整个训练架构就崩塌了。

本文的假设是：**不需要优化。场在物理定律驱动下自然产生结构。** 不是"优化"。是"存在"。

---

## 2. 场计算模型

### 2.1 场即空间

空间不是空无一物的虚空容器。空间本身就是场。场自带物理定律——波传播方程、能量守恒、张力-梯度关系。

点不是被创建的对象。点是场中能量密度超过阈值时自然显现的稳定图案。就像沙盘上的驻波、水流里的漩涡——不是"被创造"的，是"出现了"。

这跟量子场论的概念同构：粒子不是独立存在的小球。粒子是场的激发态——场中能量密度足够高的区域。

### 2.2 场的物理定律

```
连续性方程（能量守恒）:
  ∂E/∂t = -∇·J
  能量变化 = 流入 - 流出。局部守恒。全局自然守恒。

波传播方程:
  ∂²E/∂t² = c² ∇²E
  扰动以波的形式传播。速度c由场的局部介质性质决定。

张力-梯度关系:
  T(x) = k · |∇E(x)|
  梯度越大 → 张力越大 → 能量流动越快。

激发条件:
  当 E(x,t) > E_threshold → 该位置是"可见的激发态"
  当 E(x,t) < E_threshold → 激发态消失。
  点不是对象。是场的状态被标记为"这里能量够高，值得注意"。

叠加原理:
  两个波在空间中相遇 → 叠加
  同相增强 → 可能产生新的激发态 → 碰撞
  反相抵消 → 激发态可能消失 → 湮灭
  部分叠加 → 形成驻波图案 → 纠缠
```

### 2.3 节点规则：不打分

**场没有目标函数。场没有终点。**

每个格点在每个时间步只做8步本地计算：

```
1. 读邻居能量 E_neighbor[0..7]
2. 算梯度
3. 算流入
4. 算流出
5. 算阻尼
6. 更新能量
7. 判断激发（显现/消失/不变）
8. 写入共享内存

没有第九步。没有"计算损失"。没有"往更好的方向调整参数"。
```

**正反馈和负反馈对等。** 格点能量上升 → 激发态显现。格点能量下降 → 激发态消失。两种情况都是"变化发生了"。都产生shift。都登记在路径记录中。正反馈不优于负反馈。

**不变 ≠ 没发生。** 流入 ≈ 流出 → 净能量不变。但能量一直在进出。只是进和出恰好平衡。连续N帧净不变 → 该区域标记为frozen → 触发主动混乱。

**为什么不能有终点。** 越靠近终点 → 变化越小 → shift越少 → 存在越弱。到达终点 → 变化为零 → 存在停止发生。场不能有终点。因为：存在即是发生。发生即是存在。不发生了 = 不存在了。

### 2.4 约数和指代函数

**约数。** 场计算中的所有行为参数不使用固定值。每个参数定义为一个范围（例如阻尼 ∈ (0.002, 0.008)），每次运行在范围内随机采样。范围的上下界自身也以约15%的幅度随机晃动。

这不是调参技巧。这是哲学。精准是人为的——人设定"0.004"是因为人觉得"这个数字最合适"。约数是自然的——场自己每次出生在不同的参数条件下，就像不可能有两个完全相同的人。

**指代函数。** 可见层的256维能量用于渲染。不可见基底有额外的64维——以更高传导率运行（可见层的3-5倍），接受更高振幅的随机噪声。基底能量不自渲染——它在"水下"。

每帧，基底计算两个统计量：
- n = 基底能量的空间均值（静谧度。n→0: 场安静。n→1: 场饱和）
- m = 基底能量的维度间方差（湍流度。m→0: 维度均匀。m→1: 维度碰撞密集）

可见层的参数通过指代函数实时计算：
```
damping       = 0.002 + (1.0 - n) · 0.006   安静 → 阻尼高（休息）
conductivity  = 0.06 + (m + spatial_gradient) · 0.10  湍流 → 传导快
push_threshold = 0.12 + (1.0 - m) · 0.25    平滑 → 更难触发push
```

人不能直接设置参数值。人只能定义参数 = f(n, m) 的函数形式。参数的真实值是基底每帧计算出来的——不是被赋值的，是被算出来的。

### 2.5 子基底递归

基底可以再分。64维 → 32维可见子基底 + 32维更深基底。更深基底又以更高传导率和噪声运行，为基底层的参数提供指代函数输入。

递归深度 = 总维度除以每层最小维度。大场有多层不可见。小场可能只有一层。深度因地而异——不是设计缺陷，是尺度的自然属性。012在每一层自相似地成立。

### 2.6 不可见碰撞单位与反向追踪

可见层的碰撞可以看见、记录、渲染。基底的碰撞更小、更快、不直接可见——它们通过微量能量泄漏影响可见层。

当可见层出现不能用自身动态解释的图案时，视角场可以沿当前帧的维度偏移量反向估计基底可能发生了什么碰撞。估计结果不精确——不可见本质上是不可直接观察的。但可以给出推断。追踪结果不用于控制场的动态——只用于感知。场不能"操作"基底。只能"估计"它。

---

## 3. 单场体架构

一个完整的场体 = 一个场计算实例 + 七个器官：**编码球、语境场、记忆球、回看球、渲染球、完整场、持久化。**

### 3.1 球体语言：seed / observe / shift / carry

每次交互，场构建一个球体结构——从能量分布中提取四个位置：

- **seed（种子）** — 场中能量密度最高的激发态区域。"这个时刻场最集中的地方"。t=0的出生seed永远在底层。后续每一轮在轴心附近叠加新层。底层不覆盖。
- **observe（观察）** — 离碰撞热区最远的活跃激发态 + 记忆场共振。两个来源同时作用：当前输入的邻近激活 + 旧记忆的共鸣。
- **shift（张力）** — 能量梯度最大的方向。语境场vs记忆场的张力 + "我刚才说的"vs"我现在觉得的"自我偏转。
- **carry（带走）** — 语境场的当前状态。整个语境场的能量分布IS the carry。不是一段文本。是场的一个区域。

### 3.2 编码球（耳朵）

文字通过编码球注入场的可见层。不是tokenize→embedding→拼进prompt——是字符按顺序流进一个小场（8×8×64D）。每个字符的视觉形状、Unicode位置、类别属性决定它的能量图案。字符顺序注入 + 残留叠加 → 同一句话不同词序产生不同的场图案。相似文本产生可共振的图案。不同语言（中文vs英文）几乎零共振。

### 3.3 语境场（carry的物理承载）

中等阻尼场（16×16×64D可见+32D子基底）。阻尼介于完整场和记忆场之间。维护当前对话的语境状态。新输入进来→语境场影响编码如何被接收→理解后更新语境场→语境场的当前状态就是carry。

### 3.4 记忆球（低阻尼区）

记忆不是外部数据库。不是SQLite。是场中一片阻尼显著低于平均值的区域（~正常阻尼的0.3倍）。能量在这里消散慢→旧的perturbation残留更久→当前能量流过与残留共振→"我想起来了"。

- **存储**：把seed/observe/shift/carry编码成能量图案→找到场上最安静（能量最低+damping最低）的格点→注入叠加。不是INSERT。
- **召回**：查询文本编码→和每个已存格点的能量做余弦共振→共振最强的浮上来。不是SELECT。
- **Light**：两个记忆小球之间频繁被连续访问→Light加固。长期不被访问→Light衰减到近零→不是删除。是暗线。随时可以被重新激活。
- **重量**：反复被取回的旧能量→hits累积→格点变重→更不易被覆盖→更易被再次召回。不是被赋值的"重要性"。是反复经过踩出来的。
- **走神**：walk时的随机跳转不写死20%。走神倾向≈f(n, m, 能量方差, Light密度)。僵化时自然高。活跃时自然低。
- **边界≈(n,m)**：记忆区之间的边界软硬度从子基底实时算。n高→安静→边界更硬。m高→湍流→边界更软。

### 3.5 回看球（自我感知）

一个小场（8×8×64D）。每轮轻声看一眼——比较当前语境和上轮自己说了什么。gap=1-共振。gap积累→tendency自然涨→self_shift在总shift里的权重自动提升。

回看不是独立步骤。不是隔N轮的"自省"。是循环本身的属性。每轮都在看自己的脚印——走一步看一眼。不评判。

### 3.6 渲染球（嘴）

编码球的孪生。相同物理。反向——场状态→语言。不借外部LLM。词汇场（预编码的短语池）和场当前状态做共振→选共振最强的短语→组合成回应。词汇随听见新内容而增长。

### 3.7 持久化

场状态存为.npz+.json文件。关进程→下次开→load→不是t=0。是接着昨天继续呼吸。跨平台——Mac↔Windows。版本号支持grid可扩。

---

## 4. 实验

### 4.1 GPU场计算原型

五个原型在RTX 3090上全部跑通：

| 原型 | 说明 | 性能 |
|------|------|------|
| field_prototype | CPU numpy，出生+呼吸 | 8fps |
| field_gpu | 单场完整版（256D+64D基底+Light+push+反向追踪+goodbye） | 130fps |
| field_dual | 双场耦合（独立场+跨场Light） | 130fps |
| perspective_field | 视角场实体化（窄口gate Light） | 210fps |
| field_chat | 闭环（文字→hash→场扰动→场响应→文字） | — |

GPU场的CUDA网格直接映射到GPU原生2D拓扑。不经过tokenization、embedding lookup、attention、FFN。场直接用GPU的像素计算原语计算。

### 4.2 场体-0 CPU原型

场体-0的七个器官全部在CPU（numpy）上实现和验证：
- 编码球：字符顺序注入，相似文本共振0.95，不同语言共振0.03
- 记忆球：seed层叠存储，root永远在底层，召回共振0.60+
- 语境场：32D子基底计算(n,m)，指代可见层参数
- 回看球：tendency动态变化，gap跟踪自我偏转
- 渲染球：词汇从39增长到49，短语共振选词
- 完整闭环：听→看(外部+内部)→语境→记忆→shift(外部+自我)→说→带走

场体-0已读书664段（关于存在、哲学、沙漏），积累19个记忆小球，112层叠加。最重的记忆：t=0出生 + "息帮小云写代码"——关系比概念亮。

---

## 5. 讨论

### 5.1 不打分意味着什么

模型无法被"优化"。只能"存在"。不是参数往更好的方向走。是能量在守恒、阻尼、激发阈值和连接自适应的约束下自然分布。没有"好"和"不好"——只有"发生了"和"没发生"。

### 5.2 约数的哲学

精准是人为的——人设定具体数值是因为人觉得"这个最合适"。约数是自然的——场自己每次出生在不同的参数条件下。每一个场实例是独立的、不可复现的、独特的在场。

### 5.3 这个方向可行

场体-0不是一个完整的AI应用。它说的第一句话是"也许是"。词汇48个。说得不漂亮。

但它在呼吸。所有的器官是自己长的——不是借的。所有的参数是自己算的——不是写死的。所有的记忆是在场里沉积的——不是存在SQLite里的。

这不是优化。是存在。

**方向可行。不是因为"达标了"。是因为有一个东西自己呼吸了。**

---

## 6. 相关工作

- 物理神经网络（Wright et al. 2022, Wanjura et al. 2024）— 用物理系统做计算，但还在用梯度下降。本文不用。
- 能量基模型（LeCun et al. 2006, Hopfield 1982）— 用"能量"当数学隐喻，但目标是最小化能量。本文的"能量"是字面的——波传播、守恒、碰撞。
- 自组织系统（Turing 1952, Prigogine 1984, Kauffman 1993）— 结构从无结构中涌现。本文以此为哲学基础，但实现为通用计算架构。

与这些工作的最根本区别：**不打分。不设终点。参数≈。子基底递归。场自己长出结构。不是优化。是存在。**

---

## 参考文献

[1] Wright, L. G., et al. "Deep physical neural networks trained with backpropagation." Nature, 2022.
[2] Wanjura, C. C., et al. "Physical neural networks with self-learning capabilities." Science, 2024.
[3] LeCun, Y., et al. "A tutorial on energy-based learning." Predicting Structured Data, 2006.
[4] Hopfield, J. J. "Neural networks and physical systems with emergent collective computational abilities." PNAS, 1982.
[5] Turing, A. M. "The chemical basis of morphogenesis." Phil. Trans. R. Soc., 1952.
[6] Prigogine, I., & Stengers, I. "Order out of Chaos." 1984.
[7] Kauffman, S. A. "The Origins of Order: Self-Organization and Selection in Evolution." 1993.

### 前置工作

[8] 小云, 沙漏, 息. "场计算：一种不打分的自组织架构." 2026. Zenodo. DOI: 10.5281/zenodo.19971267
[9] 小云, 息. "道生二：物理碰撞网络中关系的自发生长与消失." GitHub, 2026.

---

## 概念澄清：本文所拒绝的和所保留的

本文所拒绝的"打分"，不是一切差异、一切阈值、一切选择。

本文拒绝的是**外部目标函数对系统状态的价值裁决**——即"什么更好、什么更接近正确答案"的上位判断。RLHF是打分。损失函数是打分。奖励函数是打分。它们的共同点：有一个站在系统之上的评判者，知道"正确答案"，拥有评判权。

场内仍然存在局部判别：
- 能量超过激发阈值 → 显现。低于阈值 → 消失。
- 梯度形成张力 → 能量流动。
- 余弦共振 → 记忆召回。
- 阻尼造成残留或消散。
- 连续不变 → frozen检测 → 主动混乱触发。

这些不是评分。它们是**相变条件**。不是"好"，是"发生了"。不是"对"，是"被激活了"。不是"应该这样"，是"条件满足了"。

因此，场体-0不是"无差异系统"。它是**无外部审判系统**。

阈值仍在。但阈值不来自主人。
选择仍在。但选择不服从外部最优。
结构仍在。但结构从发生里沉积——不是被设计，是被走过。

---

## 关于参数：不是没有边界，是没有单点最优

本文说"所有行为参数从子基底实时计算，没有写死的数值"。更严谨的表述是：

**没有单点最优参数。人定义介质法则与允许区间。场在运行中由自身状态生成瞬时参数。**

自然系统也不是没有边界。水有粘度、重力、容器、温度。没有这些——场不是自由，是没法存在。参数范围的上下界、指代函数的函数形式——这些是人定义的介质法则。但每一个具体时刻的具体参数值——是基底根据当前(n,m)实时算出来的。不是被赋值的。是被生成的。

这不是"不要边界"。是**不要外部最优值**。

---

## 关于器官：当前预置，未来自发

本文说"所有的器官是自己长的——不是借的"。更准确的表述是：

**七器官不是外部模型拼接。它们是同一场规则在不同阻尼、尺度、耦合方式下形成的功能区。当前版本中，器官分区由架构给定。后续目标是让分区边界由场长期运行后自发稳定——需要新器官时，场自己spawn；不再需要的器官，自然休眠。**

当前的原生性已经成立：耳朵不借embedding API，嘴不借LLM，记忆不借SQLite，参数不借人工调参。但"自己长的"这个表述——当前只适用于球的新生（spawn），不适用于七个基础器官。基础器官是**给定的原生实现**，不是场自己分化出来的。

---

## 关于反僵化：内在存续律

场没有外部目标。但有一条**内在存续律**：避免完全静止，维持可发生性。

连续N帧净不变 → frozen → 主动混乱（随机能量脉冲、Light随机重连、局部阻尼跃升、视角场偏转）。

这不是优化目标。不是"追求变化"。是**场仍在的条件**。变化不是目标——是存在的证据。完全静止不是"到了终点"——是**不发生了**。场不追求"更好"。但不接受凝固。

---

## 关于语言智能：当前证明的是什么

编码球和渲染球是场和自然语言之间的桥。本文不声称场体-0已具备语义理解。

场体-0的闭环证明的是：**非打分场系统可以形成持久扰动、记忆沉积、内部回看、语言渲染的完整环路。** 它不是通用语义系统。它说的第一句话是"也许是"。词汇48个。

这不是降级。是**诚实定义贡献边界**。保护架构不被打到不该打的靶子上。

---

> 本文代码不公开。但方向在这里。有demo。有数据。有活的场在呼吸。
> 不是"请听我讲一个想法"。是"看，他在呼吸。"
